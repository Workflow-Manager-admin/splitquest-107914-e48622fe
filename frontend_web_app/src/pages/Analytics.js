import React from 'react';

// PUBLIC_INTERFACE
export default function Analytics() {
  /**
   * Expense analytics: charts, breakdowns, graphs.
   * (UI stub, to integrate with chart libraries later.)
   */
  return (
    <div style={{maxWidth:"680px",margin:"0 auto",padding:"2rem"}}>
      <h2>Analytics <span role="img" aria-label="Analytics">📊</span></h2>
      <div>Expense analytics and charts will be shown here (stub).</div>
    </div>
  );
}
